import requests; import json; print("=== 전체 파이프라인 테스트 ==="); print("1. 현재 DB 상태 확인 완료: 9개 뉴스"); print("2. FinBERT realtime 수집 시작..."); r=requests.post("http://localhost:8000/collect/realtime", json={"limit": 1}, timeout=120); print(f"Status: {r.status_code}"); result=r.json() if r.status_code==200 else {}; print(f"수집 결과: {result.get(\"count\", 0)}개"); print("3. 새로운 뉴스 확인하기 위해 잠시 대기...")
